#include <iostream>
using namespace std;

int main()
{
    int p, r;
    cout << "Indique a posição da porta P: ";
    cin >> p;
    cout << "Indique a posição da porta R: ";
    cin >> r;
    
    if(p==0)
    cout << "A bolinha vai pelo caminho c";
    else if(p==1 && r==0)
    cout << "A bolinha vai pelo caminho B";
    else if(p==1 && r==1)
    cout << "A bolinha vai pelo caminho A";
    
    return 0;
}
